{
	"name": "@ssg/monorepo",
	"author": {
		"name": "Lukas Rosenfeldt"
	},
	"description": "One static site generator, zero dependencies.*",
	"engines": {
		"node": ">=21"
	},
	"keywords": [
		"ssg",
		"html"
	],
	"license": "MIT",
	"main": "./dist/main.js",
	"scripts": {
		"compile": "tsc --build",
		"format": "prettier --check --ignore-unknown ./ssg-zero",
		"format:write": "prettier --write --ignore-unknown ./ssg-zero"
	},
	"type": "module",
	"version": "0.4.0",
  "workspaces": [
    "./ssg-zero"
  ],
	"devDependencies": {
		"@types/node": "^20.8.10",
		"prettier": "^3.0.3",
		"typescript": "^5.2.2"
	}
}
