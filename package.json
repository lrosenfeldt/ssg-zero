{
	"name": "@ssg/monorepo",
	"author": {
		"name": "Lukas Rosenfeldt"
	},
	"description": "One static site generator, zero dependencies.*",
	"engines": {
		"node": ">=21"
	},
	"keywords": [
		"ssg",
		"html"
	],
	"license": "MIT",
	"main": "./dist/main.js",
	"scripts": {
		"compile": "tsc --build",
		"format": "prettier --check --ignore-unknown ./ssg-zero ./site",
		"format:write": "prettier --write --ignore-unknown ./ssg-zero ./site"
	},
	"type": "module",
	"version": "0.5.0",
  "workspaces": [
    "ssg-zero",
    "site"
  ],
	"devDependencies": {
		"@types/node": "^20.8.10",
		"prettier": "^3.0.3",
		"typescript": "^5.2.2"
	}
}
